import * as React from 'react';
import { Text } from 'react-native';

const ItemDetail = item => {
  // const item = props.navigation.getParam('image');

  return <Text>Hi</Text>;
};

export default ItemDetail;
